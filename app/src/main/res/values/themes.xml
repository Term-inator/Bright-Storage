<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!--style name="Theme.BrightStorage" parent="Theme.MaterialComponents.DayNight.DarkActionBar"-->
    <style name="Theme.BrightStorage" parent="Theme.MaterialComponents.DayNight.DarkActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/theme_color</item>
        <item name="colorPrimaryVariant">@color/theme_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.BrightStorage.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.BrightStorage.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BrightStorage.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="updateButton" parent="Theme.AppCompat">
        <item name="colorControlHighlight">@color/theme_color</item>
        <item name="colorButtonNormal">@color/theme_color</item>
    </style>

</resources>